// AFK poller — the bounded per-repository poller, isolated under .afk/.
// Delivered by create-afk-workflow@0.1.0.
export async function runPoller() {
  // Claim and execute the execution frontier for this repository.
}
