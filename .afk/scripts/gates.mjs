// AFK gates — resolved through the pinned runtime under .afk/, stack-neutral.
// Delivered by create-afk-workflow@0.1.0.
export async function runGates() {
  // The project adapter supplies the concrete verification contract for this repository.
}
